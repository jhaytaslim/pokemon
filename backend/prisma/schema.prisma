generator client {
    provider = "prisma-client-js"
}
datasource db {
    provider = "mongodb"
    url      = env("DATABASE_URL")
}

model Favorite {
    id   String @id @map("_id") @default(auto()) @db.ObjectId
    name String
    data Json?
}
